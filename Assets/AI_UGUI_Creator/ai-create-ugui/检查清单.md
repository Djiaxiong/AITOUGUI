# 站点包质量检查清单

## Planning

- [ ] 存在 `source/draft_visual_intent.md`
- [ ] 存在 `source/page_scope.md`
- [ ] 存在 `source/site_plan.md`
- [ ] 存在 `source/page_plans/` 目录
- [ ] `draft_visual_intent.md` 明确写出 `styleAxes`
- [ ] `draft_visual_intent.md` 明确写出材质、字体气质、题材禁止项
- [ ] `page_scope.md` 明确写出 `scopeMode`
- [ ] `page_scope.md` 明确写出游戏类型、运行模式、产品定位
- [ ] `page_scope.md` 包含页面结构树、页面清单、优先级、来源
- [ ] `page_scope.md` 明确本轮实际进入设计的页面范围
- [ ] 同类题材已明确与常见默认风格至少拉开 3 个风格轴
- [ ] 页面规划不是从旧模板、旧示例、旧案例直接改出来的

## 文件结构

- [ ] 存在 `source/ui_contract.json`
- [ ] 存在 `source/ui_self_check_report.json`
- [ ] 存在 `source/site.json`
- [ ] 存在 `source/theme.css`
- [ ] 存在 `source/shared/widgets.css`
- [ ] 根目录存在 `preview.html`
- [ ] 存在 `source/pages/` 目录
- [ ] 所有页面 HTML 都在 `source/pages/` 目录内

## Contract

- [ ] `ui_contract.json` 使用 canonical role 为主，而不是另起平行语义系统
- [ ] `site.json.pages[]` 与 `page_scope.md` 的本轮设计范围一致
- [ ] `ui_contract.json` 覆盖所有页面
- [ ] 每个页面的 `pageId / html / prefabName / targetLayer` 与 `site.json` 一致
- [ ] 每页都有根节点定义
- [ ] 每页关键命名节点都能在 HTML 找到
- [ ] 每页所有运行时绑定目标节点都在 `namedNodes` 中登记
- [ ] 所有关键按钮都在 `namedNodes` 中登记
- [ ] 所有大块中心面板都在 `namedNodes` 中登记
- [ ] 所有边缘 HUD 块都在 `namedNodes` 中登记
- [ ] 动态容器、默认尺寸 opt-in 节点、slot 节点都在 `namedNodes` 中登记
- [ ] 所有 `relativeSizeNodes` 都有合理原因
- [ ] 所有 `templateSizeNodes` 都真的带 `data-ui-template-size="true"`
- [ ] `usedRoles` 和 `usedClasses` 完整

## Self Check

- [ ] `ui_self_check_report.json` 存在
- [ ] `status == "pass"`
- [ ] `violations` 为空
- [ ] 每页 `unsupported*` 数组为空
- [ ] 每页 `missing*` 数组为空
- [ ] 每页 `undefinedClasses` 为空
- [ ] 每页 `undefinedRoles` 为空
- [ ] 每页 `browserOnlyPatterns` 为空

## HTML

- [ ] 每页根节点有 `data-ui-page`
- [ ] 每页根节点有 `data-ui-name`
- [ ] 每页根节点有 `data-ui-role`
- [ ] 每页根节点有 `class="screen-root"`
- [ ] 所有 `button` 都有 `data-ui-name`
- [ ] 所有 `button` 都有 `data-ui-role`
- [ ] 所有 `button` 都有 `data-ui-element`
- [ ] 所有运行时需要读取的文本节点都有稳定 `data-ui-name`
- [ ] 单页内没有重复 `data-ui-name`
- [ ] 没有业务命名使用 `__ai_` 前缀
- [ ] 主面板宽高明确
- [ ] 按钮宽高明确
- [ ] 卡片宽高明确
- [ ] HUD 固定块宽高明确
- [ ] 进度条轨道和填充条尺寸明确
- [ ] 页面显式链接 `theme.css`
- [ ] 页面显式链接 `shared/widgets.css`
- [ ] 页面内没有 `<style>`
- [ ] 中心主体在 `site.json.designWidth x site.json.designHeight` 设计空间中真正居中
- [ ] 左右贴边块在设计空间中真正处于边缘区域
- [ ] 装饰节点没有越出页面根画布
- [ ] 复杂交互卡片优先拆成“视觉层 + 点击层”

## 文本

- [ ] 标题、按钮、数值、统计行按单行控件处理
- [ ] 关键单行控件有明确高度
- [ ] 多行说明文案有明确宽度
- [ ] 关键文本类优先使用像素级 `line-height`
- [ ] 没有依赖文本自然撑开关键容器
- [ ] 后续需要运行时单独修改的文字，都有独立命名的文本节点

## CSS

- [ ] `theme.css` 中存在完整 `body` 规则
- [ ] `body` 规则包含 `margin:0`
- [ ] `body` 规则包含与 `site.json.designWidth` 一致的 `width`
- [ ] `body` 规则包含与 `site.json.designHeight` 一致的 `height`
- [ ] `body` 规则包含 `overflow:hidden`
- [ ] 页面根节点最终解析到 `box-sizing:border-box`
- [ ] 所有固定尺寸且带 `padding` / `border` 的关键节点最终解析到 `box-sizing:border-box`
- [ ] Unity 真实消费的视觉选择器只依赖 `:root`、`.class`、`[data-ui-role="..."]`
- [ ] `html` / `body` 只承担预览复位，不承载页面主视觉
- [ ] 语义视觉主要由 `[data-ui-role="..."]` 提供
- [ ] 通用布局主要由 `.class` 提供
- [ ] 页面独有位置和尺寸主要写在 inline style
- [ ] 没有 allowlist 之外的选择器
- [ ] 没有 allowlist 之外的属性
- [ ] `font-family` 只使用 `primary` / `heading` / `mono`
- [ ] 优先使用标准类名而不是大量新 class
- [ ] 固定尺寸 flex 容器已检查子节点 footprint，没有超出父容器内容区

## SVG 资产

- [ ] 如使用 SVG，文件全部放在 `source/assets/`
- [ ] 如使用 SVG，宿主节点写了 `data-ui-asset-id`
- [ ] 如使用 SVG，宿主节点写了 `data-ui-asset-type`
- [ ] 如使用 SVG，宿主节点写了 `data-ui-asset-usage`
- [ ] 如使用 SVG，宿主节点写了 `data-ui-asset-import`
- [ ] 如使用 Nine Slice 边框 / 外框 / 按钮底板，已补 `data-ui-asset-slice`
- [ ] 如使用 SVG 且需要稳定尺寸，已补 `data-ui-asset-width` / `data-ui-asset-height`
- [ ] 如 SVG 不应被 Unity 染色，已补 `data-ui-asset-tint="none"`
- [ ] SVG 没有承载需要运行时动态修改的文本排版
- [ ] SVG 没有依赖 `filter` / `mask` / `mix-blend-mode`
- [ ] 没有把普通纯色 panel / button / roundrect 误做成 SVG

## 受限能力使用检查

- [ ] 如果使用 `transform`，只使用 `rotate(<deg>)`
- [ ] 如果使用 `opacity`，值在 `0..1`
- [ ] 如果使用 `flex-grow` / `flex-shrink` / `flex:1`，已检查 footprint
- [ ] 如果使用 `animation`，能稳定归约到 Unity 预设或 `data-ui-loop-motion`
- [ ] 如果使用 `radial-gradient`，已接受它会被近似还原
- [ ] 没有 `flex-wrap`
- [ ] 没有 `grid`
- [ ] 没有 `calc()`
- [ ] 没有 `aspect-ratio`
- [ ] 没有 `@keyframes`
- [ ] 没有 `repeating-linear-gradient`
- [ ] 没有 `repeating-radial-gradient`
- [ ] 没有 `conic-gradient`
- [ ] 没有未受控的 `background-image`；如有，必须是显式本地 SVG 资产引用并带完整 `data-ui-asset-*` 元数据
- [ ] 没有 `clip-path`
- [ ] 没有 `mask`
- [ ] 没有 `mix-blend-mode`
- [ ] 没有 `filter`
- [ ] 没有 `backdrop-filter`
- [ ] 没有 `position:fixed`

## 工具链

- [ ] 已运行 `validate_and_prepare_repair.py`
- [ ] `validation_report.json.status == pass`
- [ ] 如有 repair prompt，已经按 prompt 修复并重跑
- [ ] 已运行 `compile_site_bundle.py`
- [ ] 根目录已生成 `compiled_site_bundle.json`
- [ ] `compile_report.json.status == pass`
- [ ] 已运行 `audit_compiled_theme_health.py`
- [ ] `theme_health_report.json.status == pass`

## 发布前最后核查

- [ ] 当前任务模式已明确为 `Single Page Text Mode` / `Single Page Image Mode` / `Full Suite Mode`
- [ ] 当前交付目标模型明确为中上能力模型，而不是继续兼容低能力模型
- [ ] 如果是 `Single Page Image Mode`，模型具备多模态能力
- [ ] 如果是 `Full Suite Mode`，`site_plan.md` 已明确 `shared invariants`
- [ ] 如果是 `Full Suite Mode`，`site_plan.md` 已明确 `page delta matrix`
- [ ] 如果是 `Full Suite Mode`，每页至少有 3 个清晰差异点
- [ ] 如果是 `Full Suite Mode`，至少 1 个差异点来自页面骨架、主导模块或交互节奏
- [ ] 没有“所有页面共用同一骨架，只是挪位置”的趋同问题
- [ ] 已选定最终展示样本范围，不再继续无限新增 case
- [ ] 每个最终展示样本都完成了浏览器预览、Unity 导入预览和 Prefab 导出核查
- [ ] 顶层 `AI_UGUI_Creator/README.md`（中文）已完成
- [ ] 顶层 `AI_UGUI_Creator/README_EN.md`（英文）已完成
- [ ] 对外带出边界已确认只含 `ai-create-ugui/` + `AIToUGUI/` + `Samples/` + 中英 README
- [ ] `Doc/` 等实现/开发文档仅留项目内，确认不随包带出

## 最终目标

- [ ] 浏览器预览正常
- [ ] Unity 导入 bundle 后可预览
- [ ] Unity 导入 bundle 后可烘焙 prefab
- [ ] 没有大面积结构错位
- [ ] 没有“compile pass 但整站 theme 退回默认值”的问题
