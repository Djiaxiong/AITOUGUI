# FPS Operator Semantic UI 任务报告

## 站点概览

- siteId: `fps_operator_semantic_ui`
- displayName: `FPS Operator Semantic UI`
- designResolution: `1920 x 1080`
- pageCount: `1`

## 正式交付入口

- HTML 预览入口: `preview.html`
- Unity 导入 JSON: `compiled_site_bundle.json`

## 目录说明

- `source/`: HTML/CSS 源包、site.json、contract、自检文件
- `reports/`: validate / compile / repair 输出
- `snapshots/`: compiled page/layout 快照

## 当前状态

- validation: `pass`
- compile: `pass`

## Unity 使用

1. 在 Unity 中选择根目录下的 `compiled_site_bundle.json`。
2. 使用 AIToUGUI 导入工具生成预览与配置资产。
3. 确认页面还原后导出 Prefab，并按需挂接代码绑定。
